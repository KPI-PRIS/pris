{
  "name": "pris-back",
  "version": "1.0.0",
  "description": "This is app for football project",
  "scripts": {
    "start:user-microservice": "cd user-service && npm run start",
    "start:gateway": "cd api-gateway && npm run start",
    "start:microservices": "concurrently \"npm run start:user-microservice\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
